// Parse the input string, trace the parser through the parse tree and display the records of the traced parse tree nodes.
(function(){
  var stats = false;
  var trace = true;
  var demo = require("./setup.js");
  demo(stats, trace);
 })();
