// Just parse the input string, collect the matched phrases and write them to the console.
(function(){
  debugger;
  var stats = false;
  var trace = false;
  var demo = require("./setup.js");
  demo(stats, trace);
 })();
